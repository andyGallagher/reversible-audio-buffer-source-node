{
    "name": "dev",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "simple-reversible-audio-buffer-source-node": "^0.0.3"
    },
    "devDependencies": {
        "vite": "^6.0.5"
    }
}
